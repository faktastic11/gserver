// import express from 'express'
// import { logApiError } from '../controllers/error'
// import { getSummaryData } from '../controllers/guidance'
// import { tickerGuidanceVal } from '../validators'

// const router = express.Router()

// router.get('/v1/summary', tickerGuidanceVal, (req, res, next) =>
//   getSummaryData(req, res, next).catch((err) => {
//     return logApiError(req, res, next, err, 500, 'Custom summary error statement')
//   }),
// )

// export default router
